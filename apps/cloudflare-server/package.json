{
  "name": "@monopoly/cloudflare-server",
  "private": true,
  "type": "module",
  "scripts": { "dev": "wrangler dev", "build": "wrangler deploy --dry-run", "test": "vitest run", "deploy": "wrangler deploy" },
  "dependencies": { "@monopoly/game-engine": "workspace:*", "@monopoly/shared-types": "workspace:*" },
  "devDependencies": { "vitest": "^2.1.4", "wrangler": "^4.77.0", "typescript": "^5.6.3" }
}
