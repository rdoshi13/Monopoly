# Browser origin permitted by CORS and Socket.IO. Any localhost origin is also
# accepted when this is itself localhost, which keeps local development working.
ALLOWED_ORIGIN=http://localhost:5173
PORT=4000
