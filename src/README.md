# Retired PeerJS implementation

This directory is not part of the active pnpm workspace. It contains the pre-migration browser-hosted PeerJS application and is retained temporarily only as reference while the workspace migration is reviewed.

Do not add fixes or features here. Active work belongs in `apps/` and `packages/`. Delete this directory in a dedicated cleanup commit after the workspace migration is safely committed.
